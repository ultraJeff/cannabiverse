extends ../../layouts/default

block content
	.container-xs
		.page-intro
			h1 Join God's Greenery
			+flash-messages(messages)
		.page-content
			+auth-forms({ modal: false, initial: 'join' })

block js
	script.
		jQuery(function($) {
			$('.auth-form').find('.form-control').eq(0).focus().select();
		});
